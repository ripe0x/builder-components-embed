import auctionAbi from "abi/auctionABI";
import govAbi from "abi/daoABI";
import tokenAbi from "abi/tokenABI";
import metadataAbi from "abi/metadataABI";

export const auctionContract = {
  abi: auctionAbi,
};
export const metadataContract = {
  abi: metadataAbi,
};
export const govContract = {
  abi: govAbi,
};
export const tokenContract = {
  abi: tokenAbi,
};